[core]
	engine = pg
	# plan_file = sqitch.plan
	top_dir = app/migrations
[engine "pg"]
	target = db:pg://user:pass@localhost:6543/db
	# registry = sqitch
	# client = psql
[target "heroku"]
	uri = db:<url postgres heroku>